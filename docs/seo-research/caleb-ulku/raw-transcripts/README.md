# Raw Transcripts

This folder is intentionally ignored by Git.

Use it only as a local holding area if raw transcript segments are needed for private analysis. Commit summarized notes instead of full transcript dumps.
